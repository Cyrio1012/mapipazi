<div class="topbar">
    <button class="menu-toggle" @click="toggleSidebar">
        <i class="fas fa-bars"></i>
    </button>
    <h4 class="mb-0">@{{ getPageTitle() }}</h4>
    <div>
        <a href="{{ route('descentes.index') }}">index</a>
        <a href="{{ route('descentes.index') }}">index</a>
        <a href="{{ route('descentes.index') }}">index</a>
    </div>
</div>